---
import { SITE } from '../config';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import RightSidebar from '../components/RightSidebar.astro';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = SITE.title,
  description = SITE.description,
} = Astro.props;

const pageTitle = title === SITE.title ? SITE.title : `${title} · ${SITE.title}`;
const canonical = new URL(Astro.url.pathname, Astro.site ?? 'https://mikulovesuki.github.io/');
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <!-- 主题引导：避免刷新闪烁（存储偏好 → 系统偏好） -->
    <script is:inline>
      (function () {
        try {
          var saved = localStorage.getItem('oblivion-theme');
          var dark = saved ? saved === 'dark' : window.matchMedia('(prefers-color-scheme: dark)').matches;
          if (dark) document.documentElement.classList.add('dark');
        } catch (e) {}
      })();
    </script>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" type="image/png" href="/favicon.png" />
    {
      Astro.url.pathname === '/' && (
        <link rel="preload" as="image" href="/img/hero-dusk.jpg" />
      )
    }
    <link rel="sitemap" href="/sitemap-index.xml" />
    <link rel="alternate" type="application/rss+xml" title={SITE.title} href="/rss.xml" />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={pageTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:site_name" content={SITE.title} />
    <meta property="og:locale" content="zh_CN" />

    <meta name="theme-color" content="#f6f8fb" />
  </head>
  <body class="flex min-h-screen flex-col">
    <Navbar />
    <div class="flex flex-1 flex-col">
      <!-- 全宽区域（如首页横幅），由页面通过 slot="hero" 填充 -->
      <slot name="hero" />
      <!-- 双栏布局：内容区 + 右侧栏（lg 以下自动收成单栏） -->
      <div class="mx-auto flex w-full max-w-6xl flex-1 items-start gap-10 px-4 py-12 sm:px-6">
        <main class="min-w-0 flex-1">
          <slot />
        </main>
        <aside class="sticky top-[72px] hidden max-h-[calc(100vh-92px)] w-72 shrink-0 overflow-y-auto pb-6 lg:block">
          <RightSidebar />
        </aside>
      </div>
    </div>
    <Footer />
    <!-- 站点吉祥物（二次元彩蛋，点击回「关于」页） -->
    <a href="/about/" title="来认识一下站长的二次元分身" aria-label="关于我" class="mascot-fixed">
      <img src="/favicon.png" alt="mascot" width="256" height="256" />
    </a>
  </body>
</html>
