# 复制为 .env 后按需修改（.env 已被 git 忽略）
# 管理后台（/admin）的访问密码。未设置时使用默认值 111。
# 生产部署前请务必修改为强密码！
# 建议修改为强密码：修改后重启 dev server 生效。
ADMIN_TOKEN=111

# 可选：GitHub Personal Access Token（仅需 public_repo 只读），
# 本地 dev 时用它拉取仓库/star 数据（限流 5000/小时，未设置则匿名 60/小时）。
# GitHub Actions 构建时自动注入 GITHUB_TOKEN，无需配置。
# GITHUB_TOKEN=ghp_xxx
